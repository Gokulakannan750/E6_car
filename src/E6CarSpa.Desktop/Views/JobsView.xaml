<UserControl x:Class="E6CarSpa.Desktop.Views.JobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quotations &amp; Invoices" Style="{StaticResource H1}" />

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Search (name / phone / car / invoice no.)" />
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" KeyDown="Search_KeyDown" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="Refresh" VerticalAlignment="Bottom" Margin="0,0,0,16"
                            Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" />
                </Grid>

                <!-- Status filter as tabs: the selected one is highlighted and filters the list
                     (same binding the dropdown used, so the behaviour is unchanged). -->
                <ListBox Grid.Row="1" ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}"
                         Background="Transparent" BorderThickness="0" Padding="0" Margin="0,4,0,0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="tab" Background="Transparent" CornerRadius="6"
                                                BorderBrush="{StaticResource OutlineBrush}" BorderThickness="1"
                                                Padding="18,8" Margin="0,4,8,0">
                                            <TextBlock x:Name="txt" Text="{TemplateBinding Content}"
                                                       FontSize="14" FontWeight="SemiBold" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="tab" Property="Background" Value="{StaticResource PrimaryBrush}" />
                                                <Setter TargetName="tab" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                                <Setter TargetName="txt" Property="Foreground" Value="White" />
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsMouseOver" Value="True" />
                                                    <Condition Property="IsSelected" Value="False" />
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="tab" Property="Background" Value="#F1F5F9" />
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding Jobs}" SelectionMode="Single"
                      MouseDoubleClick="Jobs_DoubleClick"
                      HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding CreatedAt, StringFormat='dd-MM-yyyy HH:mm'}" Width="130" MinWidth="115" />
                    <DataGridTextColumn Header="Invoice No." Binding="{Binding InvoiceNumber}" Width="95" MinWidth="80" />
                    <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="1.4*" MinWidth="120" />
                    <DataGridTextColumn Header="Car No." Binding="{Binding CarNumber}" Width="90" MinWidth="80" />
                    <DataGridTextColumn Header="Car Model" Binding="{Binding CarModel}" Width="1*" MinWidth="100" />
                    <DataGridTemplateColumn Header="Status" Width="100" MinWidth="95">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#F5F5F5" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Quotation">
                                                    <Setter Property="Background" Value="#FFF3E0" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                                    <Setter Property="Background" Value="#E3F2FD" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Invoiced">
                                                    <Setter Property="Background" Value="#E8EAF6" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                    <Setter Property="Background" Value="#E8F5E9" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                    <Setter Property="Background" Value="#FFEBEE" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#616161" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Quotation">
                                                        <Setter Property="Foreground" Value="#E65100" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                                        <Setter Property="Foreground" Value="#1565C0" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Invoiced">
                                                        <Setter Property="Foreground" Value="#283593" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                        <Setter Property="Foreground" Value="#2E7D32" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                        <Setter Property="Foreground" Value="#C62828" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Total ₹" Binding="{Binding GrandTotal, StringFormat='{}{0:N2}'}" Width="95" MinWidth="80" />
                    <DataGridTextColumn Header="Balance ₹" Binding="{Binding Balance, StringFormat='{}{0:N2}'}" Width="95" MinWidth="80" />
                    <DataGridTemplateColumn Width="72">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Open" Padding="8,4"
                                        Command="{Binding DataContext.OpenCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
