<Window x:Class="E6CarSpa.Desktop.Views.EditAssignmentView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 SizeToContent="WidthAndHeight" ResizeMode="NoResize"
 WindowStartupLocation="CenterOwner" MinWidth="500" MaxWidth="600"
 Title="Edit Assignment">
 <Grid Margin="20">
 <Grid.RowDefinitions>
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 <RowDefinition Height="Auto" />
 </Grid.RowDefinitions>

 <TextBlock Grid.Row="0" Text="Edit Daily Assignment" Style="{StaticResource H2}" Margin="0,0,0,16"/>

 <TextBlock Grid.Row="1" Text="Attendance" Style="{StaticResource Label}" />
 <ComboBox Grid.Row="2" ItemsSource="{Binding AttendanceOptions}" SelectedItem="{Binding AttendanceStatus}"
 Height="38" VerticalContentAlignment="Center" Margin="0,0,0,10" />

 <Grid Grid.Row="3" Margin="0,0,0,10">
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="*" />
 </Grid.ColumnDefinitions>
 <StackPanel Grid.Column="0">
 <TextBlock Text="Date" Style="{StaticResource Label}" />
 <DatePicker SelectedDate="{Binding AssignmentDate}" Height="38" VerticalContentAlignment="Center" />
 </StackPanel>
 </Grid>

 <Grid Grid.Row="4" Margin="0,0,0,10">
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="*" />
 <ColumnDefinition Width="*" />
 <ColumnDefinition Width="*" />
 </Grid.ColumnDefinitions>
 <StackPanel Grid.Column="0" Margin="0,0,8,0">
 <TextBlock Text="Vehicles Attended" Style="{StaticResource Label}" />
 <TextBox Text="{Binding VehiclesAttended}" />
 </StackPanel>
 <StackPanel Grid.Column="1" Margin="0,0,8,0">
 <TextBlock Text="Vehicles Completed" Style="{StaticResource Label}" />
 <TextBox Text="{Binding VehiclesCompleted}" />
 </StackPanel>
 <StackPanel Grid.Column="2">
 <TextBlock Text="Amount (INR)" Style="{StaticResource Label}" />
 <TextBox Text="{Binding AmountGenerated}" />
 </StackPanel>
 </Grid>

 <TextBlock Grid.Row="5" Text="Remarks" Style="{StaticResource Label}" />
 <TextBox Grid.Row="6" Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

 <TextBlock Grid.Row="7" Text="{Binding Error}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"
 Margin="0,0,0,10" Visibility="{Binding Error, Converter={StaticResource StringToVis}}" />

 <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
 <Button Content="Cancel" Width="80" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
 <Button Content="Save" Width="80" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" />
 </StackPanel>
 </Grid>
</Window>
