<UserControl x:Class="E6CarSpa.Desktop.Views.StaffMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Maintain Staff" Style="{StaticResource H1}" />
            <TextBlock Text="Manage the list of workers available in the system." Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,16"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <Button Content="➕ Add New Staff" Style="{StaticResource PrimaryButton}" 
                        Command="{Binding AddStaffCommand}" Width="160" Margin="0,0,16,0" />
                <CheckBox Content="Show inactive" VerticalAlignment="Center"
                          IsChecked="{Binding ShowInactive}" />
            </StackPanel>
            
            <TextBlock Text="{Binding Info}" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold"
                       Margin="0,0,0,8" Visibility="{Binding Info, Converter={StaticResource StringToVis}}" />
            <TextBlock Text="{Binding Error}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"
                       Margin="0,0,0,8" Visibility="{Binding Error, Converter={StaticResource StringToVis}}" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding StaffList}" IsReadOnly="True" BorderThickness="0"
                      HeadersVisibility="Column" CanUserAddRows="False" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="250" />
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                            <Setter Property="Text" Value="Active" />
                                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Text" Value="Inactive" />
                                                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTemplateColumn Header="Actions" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✏️ Edit" Background="Transparent" BorderThickness="0"
                                            Padding="6,2" Margin="0,0,8,0"
                                            Command="{Binding DataContext.RenameStaffCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                    
                                    <Button Content="✕ Deactivate" Background="Transparent" BorderThickness="0"
                                            Foreground="{StaticResource DangerBrush}" Padding="6,2"
                                            Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}"
                                            Command="{Binding DataContext.RemoveStaffCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />

                                    <Button Content="↻ Restore" Background="Transparent" BorderThickness="0"
                                            Foreground="{StaticResource SuccessBrush}" Padding="6,2"
                                            Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"
                                            Command="{Binding DataContext.RestoreStaffCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
